feat: focus= — the operator's scope, as a preference

Measured on the real Centreon audit: the objective was "find pre-auth RCE", and 7
of the 9 never-examined hypotheses were auth-bypass. The objective's wording
steered the recon, the recon steered generation, and nothing ever narrowed it.

  /loop "只找认证前 RCE" focus=ssrf,deserialization,command-injection,arbitrary-file-write,ssti

DELIBERATELY NOT A FILTER. A pre-auth RCE is routinely reached by chaining a
finding from some other class — a post-auth write plus an auth bypass — and
refusing the other class would make exactly that chain unfindable. So:

- the GENERATE/VERIFY brief states the scope and says what to do with the rest:
    THIS RUN IS SCOPED TO: ssrf, deserialization
    Spend this round on those classes. If you find something in another class,
    RECORD it — a pre-auth RCE is often reached by chaining a finding from
    somewhere else — but do not spend this round on it.
- the SCHEDULER adds `focusBoost` (6): above testingBoost (3), because the
  operator's stated scope outranks finishing a node mid-examination, and below
  gateBoost (14), because confirming a gate is what turns a confirmed sink into a
  working exploit. A test pins that ordering.
- a node OUTSIDE the focus is still selected when nothing inside it is due.

- `AuditLoopState.focus`, carried through the ledger; absent (not empty) when the
  operator did not narrow the run.
- Unknown classes are REFUSED at start with the known list, rather than dropped:
  a typo that silently narrows nothing is a typo you never find.
- `/goal` and `/loop` usage lines and help updated.

8 new tests, including one that asserts the focus does not make a node outside it
unselectable, and one that asserts a gate still outranks it.

697 tests, 0 failures.
