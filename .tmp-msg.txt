feat: the manual PoC — a request a reader can paste and run

The PoC section showed only the EVIDENCE (why the auditor believes it). A reader
cannot act on that. What they need is the request:

  手工验证
    GET /dsview/servlet/AxisServlet HTTP/1.1
    Host: TARGET
  预期  返回 200 且页面出现 AxisServlet 的管理界面（而非 302 跳登录）

Three things, and the finding cannot be checked without all of them:

- `AttackVector.poc` — a COPY-PASTEABLE request: a curl line or raw HTTP.
- `AttackVector.pocExpected` — WHAT TO LOOK FOR. This is the half that is usually
  missing, and without it the reader runs the request and cannot tell whether it
  worked — which is how a manual PoC becomes a manual shrug.
- the evidence — a code anchor with file:line, or a command's output.

`poc` is NOT `payload`: a payload is what to inject (' OR 1=1--), a poc is the
whole thing you paste. And `entrypoint` cannot serve as either — it is prose
("any route selected by the api firewall"), which is why this is a separate field.

THE MANUAL STEP RENDERS FIRST in the fence, because it is the part a reader acts
on; the evidence below it is why the auditor believes it.

WRITING A POC DOES NOT MAKE A FINDING REPRODUCED. A request nobody sent is still
a claim, and the tier requires a command OUTPUT carrying the command that produced
it. A test pins that. If allowCommandProbes is on, the brief now says to RUN the
poc and record the output — that is what moves it from STATIC to REPRODUCED.

WHEN THERE IS NO MANUAL STEP THE REPORT SAYS SO, rather than leaving the section
blank: "未记录手工验证步骤 —— 这条发现现在只能靠读代码相信。要能自己验证，需要一条
可复制的请求和「看什么才算成功」。" A reader who cannot see that it is missing will
assume the request above was checked.

Multi-line requests are indented line by line, so a raw HTTP request stays
copy-pasteable instead of only its first line surviving.

7 new tests, including one that asserts the manual step comes BEFORE the evidence
and one that asserts a recorded poc alone does not promote the tier.

666 tests, 0 failures.
