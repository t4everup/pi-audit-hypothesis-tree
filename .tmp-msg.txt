feat: the recon brief now states the stakes

Measured on the real Centreon run: 16 paragraphs -> 4 segments -> 4 generation
rounds -> 23 hypotheses, and 92% of EVERY hypothesis that audit ever recorded came
from those 4 windows. Two came from a combination pass. ZERO came from anywhere
else — generation is segment-driven, and nothing later goes back and reads a
subsystem the note did not mention.

So the note is the ceiling on the whole audit's breadth, and it is written in one
turn. The brief said "every later hypothesis is derived from it" without saying
"and there is no second pass", which reads as one step among several.

  THIS IS THE ONLY RECON PASS, AND IT SETS THE CEILING.

  The note is split into segments, and each segment is ONE generation round.
  Generation is where hypotheses come from — nothing later goes back and reads a
  subsystem this note does not mention. So the number of segments you write here
  is the number of windows this audit will ever look through.

  **A part of the project absent from this note is a part nothing will look at.**

  Measured on a real audit: 16 paragraphs became 4 segments, and 92% of every
  hypothesis that audit ever recorded came from those 4 windows.

  COVER THE WHOLE PROJECT, AND BE GENEROUS. One paragraph per coherent thought is
  the GRANULARITY, not the BUDGET: a large application is many paragraphs, not one
  per subsystem and not sixteen for the lot. A thin note is not a cheap audit — it
  is a narrow one, and the narrowness is invisible in the report.

  Name what you did NOT look at. "I did not read the plugin loader" is a paragraph,
  and it is far better than silence: silence is indistinguishable from "I looked
  and there was nothing there", and only one of those is true.

A concrete number from a real run is worth more than an assertion about importance,
and the last point is the one that makes the gap visible: a named unexamined area
becomes a segment, a segment that produces nothing shows up in the report's
"recon segments never turned into hypotheses" list.

6 new tests, including one that asserts the added pressure did NOT turn the recon
round into a guessing round — it still forbids writing hypotheses.

703 tests, 0 failures.

This is (A) only. (B) — a coverage round that compares the files the tree cites
against the project's file list and recons the difference — is not done, on the
chance that a recon which writes 40 paragraphs instead of 16 makes it unnecessary.
